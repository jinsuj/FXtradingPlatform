import { SecurityQuestion } from './security-question.model';

describe('SecurityQuestion', () => {
  it('should create an instance', () => {
    expect(new SecurityQuestion()).toBeTruthy();
  });
});
